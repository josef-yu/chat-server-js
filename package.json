{
  "name": "chat-server",
  "version": "1.0.0",
  "description": "Chat server",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --experimental-specifier-resolution=node --exec babel-node --watch src -x \"node --harmony\" ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joseph Yu",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "uuid": "^8.3.2",
    "ws": "^8.8.1"
  }
}
